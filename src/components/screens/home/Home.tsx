import Layout from '@/components/layout/Layout';
import { FC, memo } from 'react';

const Home: FC =  memo(
  () => {
    return (
      <Layout title='Home'>
      </Layout>
    )
  },
);

export default Home